name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    steps:
      - name: 24ds2000069@ds.study.iitm.ac.in
        run: echo "Hello, world!"
